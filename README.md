# VectorMind
A scalable PDF-based RAG pipeline that ingests large documents, chunks and embeds them into a vector database, and enables context-aware search and detailed answer generation through a Streamlit UI.
